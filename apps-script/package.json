{
  "name": "apps-script",
  "scripts": {
    "lint": "eslint src",
    "build": "rm -rf dist && node build.mts && cp src/appsscript.json dist/appsscript.json",
    "deploy": "npm run build && clasp push --force"
  },
  "devDependencies": {
    "@types/google-apps-script": "^2.0.7",
    "@types/moment": "^2.11.29",
    "esbuild": "^0.25.6",
    "glob": "^10.5.0",
    "typescript": "^5.0.0"
  }
}
